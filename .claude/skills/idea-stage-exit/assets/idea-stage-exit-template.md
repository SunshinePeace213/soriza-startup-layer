---
stage: idea-stage-exit
status: decided
decision: GO   # GO | NO-GO
exit_criteria_met: { problem_real: true, solution_serves_validated: true, enough_signal: true }
---

# Idea-Stage Exit — GO / NO-GO — <slug>

## The validated problem (what discovery actually revealed)

<who · how often · how severe · what they do now — as VALIDATED by the interviews, not as originally
assumed. Note any drift from the hypothesis.>

## Exit-criteria check (the three the article requires)

1. **Is the problem real & specific?** — <yes/no + the evidence: exactly who, how often, how severe, what
   they do now>
2. **Does the solution address the VALIDATED problem?** — <per solution-design's drift audit; serves the
   discovered problem, not the assumed one>
3. **Is there enough signal to justify building?** — <the kill-criteria scorecard summary; a reasoned bet,
   not certainty — and not an act of faith>

## Kill-criteria status

- **Tripped:** <none> | <criterion — and the explicit override the founder stamped>

## Decision

**GO** → proceed to `/build-poc`.
**NO-GO** → <PIVOT (→ /sharpen-hypothesis) | KEEP-DISCOVERING (more interviews) | DROP (promote another
slate idea)> — because <reason>.

<If proceeding past a tripped criterion: `Override: founder proceeded past TRIPPED <criterion> on <ISO-date> — <reason>.`>
