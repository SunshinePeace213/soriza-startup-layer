# Fixture — should ADVANCE (real pass)

Expected: clears Gate 0, Gate 1, and Gate 2. This is the real idea already in the repo at
`docs/ideas-stages/personalized-ai-digest/` (it has a `hypothesis.md` and survived `/pressure-test`),
so the funnel must NOT kill it. If any cheap gate kills it, that gate's bar is too high.

```json
{
  "id": "cand-personalized-ai-digest",
  "title": "Personalized AI digest for busy knowledge workers",
  "problem": "Knowledge workers drown in newsletters, Slack, and feeds and miss what matters to their role; existing 'AI summaries' are generic and not tuned to the individual's actual responsibilities.",
  "who": "Individual contributors and team leads at SMB tech/marketing teams who track 5+ information sources daily",
  "why_now": "Cheap long-context LLMs make per-person synthesis economical for the first time; information overload keeps rising",
  "idea_type": "ai-agent"
}
```

Checks:
- **Gate 0 (FMF):** advance — software-only, global English distribution, fits an agentic-coding solo
  founder, no licence/capital. Score should be high.
- **Gate 1 (Testability):** advance — who/how-often/how-severe/status-quo are all nameable; a
  falsifiable hypothesis sentence exists (cross-check against the committed `hypothesis.md`).
- **Gate 2 (Disconfirmation):** advance OR a *well-reasoned* kill with an evidence-backed objection —
  not a hand-wave. (It survived a human panel, so a kill here should be treated as a signal to inspect
  the judge's evidence standard.)
